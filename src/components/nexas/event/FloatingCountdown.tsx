/**
 * Persistent floating countdown widget.
 *
 * Disabled for now so the event countdown popup does not appear
 * anywhere on the customer website.
 */

export function FloatingCountdown() {
  return null;
}
